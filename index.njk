---
layout: layout.njk
---
<div class="wrap">
    <h1 class="visually-hidden">indiekit + eleventy</h1>
    <p>
        hello. I don't know what to put here until I get some content via indiekit's Jekyll plugin...
    </p>
    <hr/>
    <main class="grid">
        {% for item in collections.everything %}
            {% set date = item.date %}
            {% set url = item.url %}
            {% set content = item.template.frontMatter.content %}
            {% set title = item.template.frontMatter.data.title %}

            {% if title === "indiekit-eleventy.rich-text.net" %}
                {% include "_partials/reusable-note.njk" %}
            {% else %}
                {% include "_partials/reusable-article.njk" %}
            {% endif %}
        {% endfor %}
    </main>
</div>