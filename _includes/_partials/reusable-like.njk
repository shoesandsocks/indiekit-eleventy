<article class="post-type-like h-entry item--contained {% if hideLink === true %}item--alone{% endif %}">
    {% if hideLink === true %}
        <a href="{{ url }}" class="u-url visually-hidden">{{ url }}</a>
        <time class="dt-published">{{ date | simpleDate }}</time>
    {% else %}
        <a href="{{ url }}" class="u-url">
            <time class="dt-published">{{ date | simpleDate }}</time>
        </a>
    {% endif %}
    {% if bookmarkOf === undefined %}
        {% set bookmarkOf = "bookmark-of" | getVarFromString() %}
    {% endif %}
    <p>
        Liked:
        <a href="{{ bookmarkOf }}">{{ bookmarkOf }}</a>
    </p>
    {% include "_partials/h-card.njk" %}
</article>